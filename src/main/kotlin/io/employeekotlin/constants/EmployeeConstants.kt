package io.employeekotlin.constants

class EmployeeConstants {
    companion object{
        val NOT_FOUND = "Employee with the Provided ID not found"
        val EMAIL_EXISTS = "Employee with the email already exists"
        val EMAIL_ALREADY_TAKEN= "Email already taken"
        val NULL_MESSAGE = null
    }
}